{% include 'header' %}

import UIKitLibrary
import TableKit

class {{ module_info.name }}Cell: BaseTableViewCell {
    
    // MARK: - Constants
    
    private enum Constants {
        
        
        
    }
    
    // MARK: - Views
    
    

    // MARK: - Properties

    

    // MARK: - Computed Properties

    
    
    // MARK: - Init
    
    override func commonInit() {
        super.commonInit()
        
        
    }
    
    // MARK: - Views
    
    override func addViews() {
        
    }
    
    // MARK: - Constraints
    
    override func configureConstraints() {
        
    }
    
    // MARK: - Appearance
    
    override func configureAppearance() {
        super.configureAppearance()
        
        
    }
    
}

// MARK: - ConfigureCell

extension {{ module_info.name }}Cell: ConfigurableCell {
    
    static let defaultHeight: CGFloat? = 44
    
    func configure(with viewModel: {{ module_info.name }}CellViewModel) {
        bind(viewModel)
    }
    
}

// MARK: - Binding

private extension {{ module_info.name }}Cell {

    func bind(_ viewModel: {{ module_info.name }}CellViewModel) {

    }

}

// MARK: - Constraints

private extension {{ module_info.name }}Cell {
    
    
    
}

// MARK: - Private

private extension {{ module_info.name }}Cell {
    
    
    
}

// MARK: - Preview

#if DEBUG

import SwiftUI

struct {{ module_info.name }}Cell_Previews: PreviewProvider {
  
    static var previews: some View {
        let cell = {{ module_info.name }}Cell()
        cell.configure(with: .init())
        
        return cell.preview
            .previewLayout(.fixed(
                width: 375,
                height: {{ module_info.name }}Cell.defaultHeight ?? .zero)
            )
    }
  
}

#endif

